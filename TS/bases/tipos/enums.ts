(() => {

    enum AudioLevel { 

        min,
        medium,
        max,

    }

    let currentAudio = AudioLevel.max

    let audioLevel = 3

    console.log(currentAudio)
    console.log( AudioLevel )

})()